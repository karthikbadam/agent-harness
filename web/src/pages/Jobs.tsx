import { Text } from "@chakra-ui/react";
import { Shell } from "../components/Shell";

export function JobsPage() {
  return (
    <Shell title="Jobs">
      <Text color="fg.muted">Jobs list — coming in step (g).</Text>
    </Shell>
  );
}
