import { Text } from "@chakra-ui/react";
import { Shell } from "../components/Shell";

export function SchedulesPage() {
  return (
    <Shell title="Schedules">
      <Text color="fg.muted">Schedules list — coming in step (i).</Text>
    </Shell>
  );
}
